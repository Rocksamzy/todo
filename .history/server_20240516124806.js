const http = require('http');

const server = http.createServer(function(req, res){

    // Create A New Todo

    // Read all todos

    // Read one todo

    // Delete one todo

});

server.listen(8000, () => {
    console.log('listening on port')
})